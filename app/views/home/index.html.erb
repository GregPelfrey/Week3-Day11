<%= link_to "Add Car", new_car_path %>

<ul>
<% @cars.each do |car|%>
	<li>
		<%= car.make %>
		<%= car.model %>
		<%= car.year %>
		<%= car.color %>
		<%= link_to "Show", car_path(car) %>
		<%= link_to "Edit", car_path(car) %>
	</li>
<% end %>
</ul>
